// This script was intentionally removed. Use the web setup page at `/setup` to create the
// initial admin account. Having a CLI fallback caused confusion and inconsistent behavior
// during initial deployments, so it has been deprecated.

// If you need a one-off script for manual environments, please create a new script
// outside of the repository or coordinate with the devops team. Do NOT re-add a
// repository-scoped CLI that manipulates production data without review.

console.log('scripts/create-initial-admin.js is removed - use /setup page instead.');
